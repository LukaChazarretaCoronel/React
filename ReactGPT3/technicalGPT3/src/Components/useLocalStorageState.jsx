// Write a custom hook called useLocalStorageState that syncs a piece of state with the browser
// 's localStorage. The hook should accept a key and an initial value, and should return the state and a function to update it. 
// Ensure that the state is initialized from localStorage if a value exists and updates localStorage when the state changes.
// useReducer with Asynchronous Actions

export function useLocalStorageState(){

}
